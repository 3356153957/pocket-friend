# Pocket Friend 匹配机制专项PRD
## 文档基础信息
- 产品：Pocket Friend（8bit像素风可穿戴社交硬件，黑客松项目MVP）
- 文档版本：v1.0
- 编写日期：2026-07-24
- 所属模块：App用户画像、硬件近距离匹配引擎、捕捉准入规则
- 依赖文档：pocket-friend-prd.md、pocket-friend-overview.md
- 目标读者：前端、硬件开发、后端、设计、测试
- 优先级：P0（MVP核心，黑客松现场Demo必备）

## 一、模块背景与需求目标
### 1.1 原有匹配机制痛点
原PRD仅基于**兴趣标签重合度**做单一维度匹配，针对黑客松/独立创作者场景存在明显短板：
1. 仅靠技能标签匹配，忽略协作性格适配，容易出现技能互补但沟通不合的组队矛盾；
2. 用户标签录入方式单一，缺少自动生成、快速测评路径，注册流失高；
3. 匹配只看标签重合，缺少**互补匹配权重**分层，无法满足黑客松“补齐团队短板”核心诉求；
4. 无完整用户画像分层，硬件雷达震动、匹配优先级无差异化排序逻辑。

### 1.2 本次新增需求概述
在原有标签匹配基础上，新增**MBTI人格维度**作为第二大匹配因子，构建「MBTI性格适配 + 项目/技能标签」双向加权匹配体系；完善注册画像录入流程，支持自选MBTI/10题简易测评自动生成MBTI、手动/自动填充项目标签，适配黑客松组队、线下创作者偶遇场景。

### 1.3 模块核心目标
1. 构建双维度匹配模型：**性格适配分 + 项目标签匹配分**加权计算综合匹配度；
2. 降低注册门槛：MBTI双录入模式、标签支持手动输入+推荐选择，减少填写成本；
3. 适配黑客松场景：强化技能互补匹配权重，优先推送能补齐团队短板的用户；
4. 硬件联动：匹配分数直接控制雷达脉冲强度、震动梯度、屏幕提示优先级；
5. 完整闭环：匹配成功→捕捉仪式→家园生成对应像素小人+专属家具，和原有家园体系打通。

### 1.4 成功衡量指标（Demo&线上）
1. Demo指标：现场双人匹配触发成功率≥95%，观众可清晰理解“性格+技能双维度匹配逻辑”；
2. 产品指标：
   - 注册完成画像填写率≥70%；
   - 高匹配分（≥75分）用户捕捉成功率提升30%；
   - 捕捉后7天回访小岛查看互动剧情留存≥40%；
   - 单向扫描确认通过率提升25%。

## 二、目标用户与用户故事
### 2.1 核心用户画像（复用原有产品用户）
1. 黑客松参赛学生/独立开发者：急需快速找到技能互补、性格合拍的队友；
2. 硬件/AI/设计创作者：线下展会、市集寻找共创合伙人；
3. 泛社交年轻人：厌倦线上交友，希望线下偶遇同频、沟通顺畅的人。

### 2.2 用户故事
1. 作为第一次使用的黑客松参赛者，我不清楚自己的MBTI，希望10道简易测试快速生成人格类型，不用做长问卷，快速完成注册画像；
2. 作为后端开发者，我正在做AI硬件项目，希望系统优先匹配懂硬件结构、擅长UI设计的互补型用户，同时性格适配，方便组队协作；
3. 作为社恐内向型创作者，希望匹配同频性格的人，降低线下沟通压力；
4. 作为佩戴硬件的用户，希望靠近高匹配度人群时设备震动更强、雷达提示更醒目，快速定位合拍伙伴；
5. 作为被单向扫描的用户，希望App展示对方匹配分数、性格与标签重合点，辅助我判断是否同意捕捉。

## 三、全局名词定义
| 名词 | 释义 |
|------|------|
| 用户画像二元维度 | 维度1：MBTI人格类型；维度2：项目/技能标签（用户正在做什么） |
| MBTI适配分 | 基于16型人格协作兼容库计算的0-50分性格匹配得分 |
| 标签匹配分 | 由标签重合度+标签互补度加权计算的0-50分技能匹配得分 |
| 综合匹配分 | MBTI适配分（50%权重）+标签匹配分（50%权重），总分0-100 |
| 高匹配人群 | 综合匹配分≥70分，10米内硬件强震动、高频雷达脉冲 |
| 中匹配人群 | 综合匹配分40-69分，中等震动、低频脉冲 |
| 低匹配人群 | 综合匹配分＜40分，无震动、雷达不展示 |
| 互补标签 | 标签逻辑互洽、可补齐项目能力短板，例：#后端开发 ↔ #工业设计、#AI算法 ↔ #硬件外壳 |
| 重合标签 | 双方完全一致的兴趣/项目标签，例：双方都带#黑客松、#嵌入式 |

## 四、P0核心功能：用户画像录入模块（注册登录流程）
### 4.1 流程总览
用户登录App → 进入「个人画像设置页」→ 分两步填写：①MBTI人格 ②当前项目/技能标签 → 保存画像数据，同步至硬件设备本地缓存。

### 4.2 功能1：MBTI人格录入（双模式可选）
#### 4.2.1 模式A：直接选择（快速模式，默认首选项）
1. 页面展示16种MBTI类型缩略卡片，附带极简协作性格说明（适配黑客松组队视角）；
2. 用户单选1种类型，点击确认直接保存，无需答题；
3. 支持随时返回修改MBTI类型。

#### 4.2.2 模式B：10题简易测评（未知自身MBTI用户入口）
1. 入口：页面底部「不知道自己MBTI？点击测试」按钮；
2. 题库规则：固定10道职场/协作向选择题，覆盖E/I、S/N、T/F、J/P四维度，每题二选一；
3. 计分逻辑：四维度分别统计倾向分值，自动判定最终16型结果；
4. 结果页：展示MBTI类型+简短协作优势描述，一键回填至画像；
5. 约束：MVP固定内置题库，不支持自定义题目，本地计算结果，无需联网。

#### 4.2.3 底层依赖
对接内置MBTI协作适配数据库：存储16型两两之间的兼容系数，用于后续匹配分计算，本地JSON存储，无需第三方接口。

### 4.3 功能2：当前项目/技能标签录入（双填充模式）
标签上限：每人最多选择/填写5个标签，和原PRD保持一致。
#### 4.3.1 模式A：推荐标签选择（黑客松场景预置标签库）
预置分类标签，支持多选：
1. 技术类：#前端、#后端、#嵌入式、#AI大模型、#硬件开发、#电路设计；
2. 创作类：#工业设计、#像素美术、#音乐制作、#视频剪辑；
3. 需求类：#找合伙人、#组队黑客松、#寻找共创、#寻求投资；
4. 场景类：#AdventureX、#线下市集、#共享办公。

#### 4.3.2 模式B：手动自定义输入
用户输入自定义标签，自动添加#前缀，过滤特殊符号、长度限制2-10字；自定义标签参与重合、互补分计算。

#### 4.3.3 自动填充扩展（P0兜底，P1优化）
MVP阶段简化实现：暂不接入GitHub/项目文档自动识别标签，预留接口设计；Demo使用手动选择模式完成演示。

### 4.4 数据同步规则
1. 用户画像（MBTI类型、全部标签）本地持久化存储；
2. 设备蓝牙连接App时，自动同步画像至硬件ESP32/树莓派本地缓存；
3. 硬件离线状态下，使用缓存画像完成近距离匹配计算，无需联网。

## 五、P0核心功能：双维度加权匹配计算引擎
### 5.1 匹配总分计算公式（总分0-100）
综合匹配分 = MBTI适配分（0-50） + 标签匹配分（0-50）
#### 5.1.1 MBTI适配分计算规则（0-50分）
1. 内置16型人格协作兼容矩阵，矩阵数值区间0-50；
2. 矩阵逻辑：
   - 高度适配（互补协作型）：40-50分（例：INTJ × ESFP、INFP × ESTJ）；
   - 中性适配（沟通无冲突）：20-39分；
   - 低适配（协作矛盾概率高）：0-19分；
3. 计算方式：读取双方MBTI组合对应固定分值，直接作为人格适配分。

#### 5.1.2 标签匹配分计算规则（0-50分）
标签分由「重合标签分（0-25）+互补标签分（0-25）」相加：
1. 重合标签分：共同标签数量 × 5，上限25分；
2. 互补标签分：命中预置互补标签对数量 × 5，上限25分；
3. 示例：双方2个重合标签、2组互补标签 → 重合10分+互补10分=标签总分20分。

#### 5.1.3 匹配分层阈值（联动硬件交互）
| 综合匹配分区间 | 硬件震动反馈 | 屏幕雷达UI | 匹配优先级 |
|----------------|--------------|------------|------------|
| 70-100 高匹配 | 高频强震动，距离越近震动幅度越大 | 红色雷达脉冲、大字「同频绝佳伙伴」 | 最高，优先展示 |
| 40-69 中匹配 | 低频中等震动 | 黄色雷达脉冲、小字「兴趣匹配」 | 次优先 |
| 0-39 低匹配 | 无震动，雷达不加载对方光点 | 不展示对方雷达标记 | 不触发匹配提醒 |

### 5.2 硬件近距离匹配触发逻辑（复用原有10米范围）
1. 硬件持续广播本机画像精简数据（MBTI缩写+标签ID列表）；
2. 10米内收到其他设备广播包，本地独立计算综合匹配分；
3. 仅≥40分设备在雷达页面显示光点，≥70分触发震动提醒；
4. 多设备同时在场时，按综合匹配分从高到低排序雷达光点层级，高分用户光点更大、闪烁更快。

### 5.3 捕捉准入校验规则（匹配分前置校验）
1. 双方同时举起设备触发捕捉流程时，系统读取实时综合匹配分；
2. 无硬性拦截：低分用户仍可手动完成捕捉（尊重用户自主社交选择权）；
3. 单向扫描场景：向被扫描用户弹窗展示匹配摘要：「对方与你匹配68分｜INTP×ENTP｜共同标签#黑客松，互补标签#AI+#硬件」，由用户选择同意/拒绝。

## 六、匹配结果联动下游模块规则
### 6.1 家园像素小人生成规则
1. 小人基础形象：由双方MBTI组合决定基础肤色、发型、性格动作（内向MBTI小人常静坐，外向MBTI主动走动）；
2. 家园家具生成：重合标签生成通用共创家具，互补标签生成专属特色道具；
例：#后端（重合）→ 代码工作台；#AI × #硬件（互补）→ 嵌入式调试台。

### 6.2 小人详情页匹配信息展示
点击小岛像素小人，新增匹配信息板块：
1. 综合匹配分数；
2. 双方MBTI类型+简短协作适配解读；
3. 重合标签清单、互补标签清单；
4. 相遇提示文案示例：「7月23日 Adventure X，INTP & ENTP，因#硬件、#AI互补相遇」。

### 6.3 NPC行为系统联动（P1）
匹配分越高，岛上两个小人互动剧情触发概率越高：
- 高匹配：高频共同讨论、协作工作、合影回忆剧情；
- 低匹配：极少互动，各自独立活动。

## 七、Demo专属Mock适配方案（黑客松现场必做）
1. 预置3组测试用户画像，覆盖高/中/低三档匹配分，现场可一键切换；
2. 硬件支持手动模拟匹配分，无需真实蓝牙测距，保障演示稳定；
3. 捕捉成功弹窗、App小岛页面固定展示MBTI匹配逻辑说明，方便评委理解；
4. 兜底方案：网络异常时，全部匹配计算本地离线完成，不依赖云端。

## 八、隐私与权限约束
1. MBTI、标签仅广播精简ID，不明文传输完整文字，保护用户隐私；
2. 离线模式下，设备停止广播画像，不会被任何设备匹配扫描；
3. 用户可随时删除/修改MBTI、标签，修改后实时同步硬件，匹配规则即时更新；
4. 无强制匹配，低分用户不推送提醒，减少社交打扰。

## 九、优先级划分（MVP范围界定）
### P0（本次黑客松必须实现）
1. MBTI双录入模式：直接选择 + 10题简易测评；
2. 标签手动选择/自定义录入，上限5个；
3. 本地MBTI兼容分、标签重合/互补分加权计算引擎；
4. 匹配分分层联动硬件震动、雷达UI；
5. 捕捉页面、小岛详情页展示匹配维度信息；
6. Demo预置Mock用户画像，支持现场演示。

### P1（黑客松结束后迭代，本次不开发）
1. 标签自动识别填充（GitHub/项目文本NLP提取）；
2. 用户匹配行为反馈优化权重（捕捉/拒绝行为调整匹配系数）；
3. 自定义匹配偏好：用户可自主调高性格/标签单项权重；
4. 多人组队综合匹配，输出团队互补报告。

### P2（远期扩展，本次不做）
1. MBTI深度八维匹配模型；
2. 地理位置标签加权匹配；
3. 付费高级匹配：细分行业、创业阶段定向筛选。

## 十、异常流程与边界规则
1. 用户未填写MBTI：默认分配中性适配类型ISTP参与匹配，登录强制弹窗引导完善画像；
2. 用户标签为空：标签匹配分固定0分，仅依靠MBTI分计算综合匹配度；
3. 多台设备同处10米内：雷达按匹配分排序，高分置顶，无数量上限；
4. 硬件断连App：使用上一次同步的缓存画像匹配，重连后自动同步最新数据；
5. 测评中途退出：不保存半成品MBTI结果，返回画像首页。

## 十一、验收标准（可测试用例）
1. 用户可直接选择16型MBTI，或完成10题测试自动生成类型；
2. 用户可选择预置标签/自定义标签，最多5个；
3. 两台设备靠近，本地正确计算综合匹配分，分层触发对应震动与雷达；
4. 高匹配分用户捕捉成功后，小岛生成对应MBTI风格小人、重合/互补标签家具；
5. 单向扫描时，弹窗展示完整匹配维度信息；
6. 离线模式下，设备停止广播画像，无任何匹配提醒；
7. Demo预置用户可一键切换，完整演示高匹配双人捕捉全流程。

## 十二、非功能需求
1. 性能：硬件本地匹配计算单次耗时≤50ms，无卡顿；
2. 离线可用：全部匹配逻辑、MBTI库、标签库本地存储，断网不影响；
3. 兼容性：适配ESP32/树莓派Zero硬件，Flutter/React Native App；
4. 存储：用户画像数据本地轻量化存储，单条数据≤1KB。

## 十三、待确认事项
1. MBTI协作兼容矩阵具体分值表定稿；
2. 黑客松互补标签完整映射清单；
3. 10道MBTI测评题目最终定稿；
4. App硬件端匹配分UI展示视觉规范。
